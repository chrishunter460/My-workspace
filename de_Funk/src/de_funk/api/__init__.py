"""
de_funk API — FastAPI backend for the Obsidian plugin.

Resolves domain.field references to Silver Delta Lake tables and executes queries.
Binds to 0.0.0.0:8765 by default.

Install: pip install de_funk[api]
Run:     python -m scripts.serve.run_api
"""
