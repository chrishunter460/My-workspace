name: de_funk
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - setuptools
  - wheel
  - pip:
    - -e ".[spark,api,dev]"
    - great_tables>=0.12.0
    - markdown>=3.4.0
    - statsmodels>=0.14.0
    - scikit-learn>=1.3.0
    - deltalake>=0.14.0
    - python-dotenv>=1.0.0
    - numpy
